#include <stdio.h>

int main(){
	int num1,cent,dobro;
	printf("Base do quadrado:");
	scanf("%d",&num1);
    cent = num1*num1;
    dobro = cent*2;
	printf("area do quadrado: %d, dobro da area: %d",cent,dobro);
}
