#include <stdio.h>
int main(){
    int soma,number;
    soma = 0 + scanf("%d",&number);
    soma = soma + scanf("%d",&number);
    soma = soma + scanf("%d",&number);
    soma = soma + scanf("%d",&number);
    soma = soma + scanf("%d",&number);
    
    printf("Soma das notas: %d", soma);
    
}